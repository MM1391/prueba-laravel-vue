Esto es un CRUD básico con Laravel y VueJS.

Tiene paginación y autenticación con Vue.

Para instalar:
-crear proyecto nuevo de Laravel
-instalar laravel ui (composer require laravel/ui)
-instalar ui vue (php artisan ui vue --auth)
-npm i
